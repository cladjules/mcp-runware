# Runware API Configuration
RUNWARE_API_KEY=your_api_key_here

# MCP Server Configuration
# Transport mode:
#  - stdio (default, for Claude Desktop)
#  - http (for remote access with Express server)
#  - vercel (for Vercel serverless deployment - don't start server)
MCP_TRANSPORT=stdio

# HTTP Server Settings (only used when MCP_TRANSPORT=http)
PORT=3000
HOST=127.0.0.1

# API Key Authentication (HTTP mode only)
# Comma-separated list of valid API keys for client authentication
# Leave empty to disable authentication (not recommended for production)
# Example: MCP_API_KEYS=key1-abc123,key2-def456,key3-ghi789
MCP_API_KEYS=
